## **3D Mesh Manager**

Python program that:

Lets you upload several mesh files(.obj) in specified path and stores its metadata 
(name, number of vertices, faces, dimensions-bounding box) in txt.

Is OOP. Keeps this info in a class-based structure.

Unit tests included.









